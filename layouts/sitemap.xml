{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages -}}
    {{- $skip := false -}}
    {{- /* 분류 루트 목록(/tags/ /series/ /categories/)은 사이트맵에서 제외 */ -}}
    {{- if eq .Kind "taxonomy" }}{{ $skip = true }}{{ end -}}
    {{- /* 모든 태그 페이지(루트 + 개별 term)는 thin content라 제외 */ -}}
    {{- if hasPrefix .RelPermalink "/tags/" }}{{ $skip = true }}{{ end -}}
    {{- /* 검색 기능 페이지는 색인 가치가 없어 제외 */ -}}
    {{- if eq .RelPermalink "/search/" }}{{ $skip = true }}{{ end -}}
    {{- if and .Permalink (not $skip) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
    {{- end -}}
  {{- end }}
</urlset>
